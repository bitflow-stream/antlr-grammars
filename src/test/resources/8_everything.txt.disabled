{
    http_input(port=7000);
    file_in(path="/var/log/access.log");
}
-> someTransform(tag="tag1")
-> someFork(nrOfThreads=5){
         raw:       /* some ugly comment */ stepA1()[metadata="nothing"] -> stepA2();
         "default": stepB1() -> stepB2(); // this is the default case
       }[ mode="offline" ]
-> outputFork(mode="round_robin") [server_tag="log-server"] {
          http_output(port=7001);
          file_out(path="/var/log/access.log");
   }